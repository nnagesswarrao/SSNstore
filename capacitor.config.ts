import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sns.stores',
  appName: 'SNS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
